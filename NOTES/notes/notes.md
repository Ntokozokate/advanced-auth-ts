# Rule for Mid-Level Engineers: Always copy-paste .env keys. Never type them by hand.

# find() vs findOne()

find() returns an array. This is good for selecting a group eg all members from Harare

- also makesure to use array methods to deal with the result

findOne() returns a single object or null. Perfect for selecting a single user
